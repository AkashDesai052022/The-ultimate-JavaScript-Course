alrert('hello world')
let p = prompt("enter a number", '24')  // last is default value;

confirm("would you like to proceed?")  // cancel = fasle ; ok = true;

// use madals instead of alert prompt, confirm
