// window , BOM AND DOM

console.log(window);
console.log(document);
console.log(document.body);
document.body.style.backgroundColor = 'yellow'


// Dom = document object model
// bom = browser object model  = provides additional objcet by browser (host environment) alert prompt confirm is part of bom